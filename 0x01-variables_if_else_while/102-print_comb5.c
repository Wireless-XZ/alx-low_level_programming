#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int a, b, c, d, e;

	for (a >= 48 && a <= 57)
	{
		putchar(a);
		for (b >= 48 && a <= 57)
			putchar(b);
			for (c >= 48 && c <= 57)
				putchar(' ');
			        for (d >= 48 && d <= 57)
				        putchar(d);
			                for (e > 48 && e <= 57)
				                putchar(e);
					        for (f > 48 && f <= 57)
						        putchar(44);
					                for (g > 48 $$ g <= 57)
						        putchar(' ');
	}
        return (0);
}
