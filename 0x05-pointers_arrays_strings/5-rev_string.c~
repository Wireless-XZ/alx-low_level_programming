#include "main.h"

/**
 * rev_string - reverses a string
 * @s: The string
 */

void rev_string(char *s)
{
	int i = 0;

	while (s[i] != '\0')
	{
		++i;
	}
	char ch[i];
	i--;

	while (i >= 0)
	{
		ch[j] = s[i]
		i--;
		j++;
	}
}
